# 隐私说明

## 处理范围

本工具只应处理使用者本人已登录、已授权的本机微信账号。使用者必须明确选择
聊天对象、时间范围和消息类型；附件复制需要额外确认。

## 本机数据访问

- 普通发现阶段只读取文件和进程元数据，不读取聊天正文。
- 不知道数据库密钥时，工具可在明确授权后对正在运行的 Weixin 进程做有范围、
  有时间限制的只读检查，并用目标数据库的 SQLCipher HMAC 验证候选密钥。
- 工具不写入进程、不注入代码、不保存内存转储，也不读取无关进程。
- 解密数据库仅在临时清理范围内存在；任务结束或失败后删除。
- 密钥不写入磁盘、日志、命令行、报告或导出文件，并对可变缓冲做尽力擦除。

## 网络与 AI

导出运行时默认不需要网络，也不会自动上传微信数据库或聊天记录。只有本地表情
缓存无法识别、用户再次明确同意时，工具才可访问该条消息自带的受限微信 CDN
地址；该模式默认关闭，并限制域名、大小和图片签名。生成的 ZIP
保留在用户指定的本机目录。

Codex 本身是联网 AI 产品；用户在对话中输入的内容以及主动上传给 AI 的导出
文件，受其所使用产品和账号的数据设置、隐私政策及组织政策约束。本工具无法
控制用户之后把导出文件发送到哪里。处理敏感工作内容前，应先确认允许使用的
AI 服务和数据保留设置。

## 输出内容

导出包可能包含：

- 被选择的聊天正文；
- 发送者显示名和时间；
- 用户明确选择的附件；
- 筛选范围、数量、文件哈希和不可用媒体状态。

导出包不会包含数据库密钥、内存转储、绝对源路径或未获准的附件。

## 诊断与问题报告

报告问题时请只提供脱敏错误、工具版本和微信版本。不要公开上传：

- 微信数据库、快照或聊天导出；
- 任何数据库或媒体密钥；
- `wxid`、手机号、账号目录名或完整私人路径；
- 含聊天正文的截图和日志。
