# 安全策略

## 支持版本

安全修复优先应用于最新正式发行版。旧版本仅保留用于回退，不保证继续维护。

## 报告安全问题

如果仓库启用了 GitHub Private Vulnerability Reporting，请优先使用仓库
**Security → Report a vulnerability** 私下报告。

如果没有该入口，请创建一个不含漏洞细节和私人数据的普通 Issue，只说明
“需要私下报告安全问题”，等待维护者提供私密沟通方式。

请勿在公开 Issue 中发布密钥、聊天正文、数据库、内存内容、账号标识或可直接
复现他人数据访问的细节。

## 设计边界

本项目明确不提供或接受以下功能：

- 写入微信进程、DLL 注入、Hook 或远程线程；
- 自动结束微信进程；
- 保存完整内存转储或宽泛扫描无关进程；
- 绕过 Windows 账号权限或访问他人微信账号；
- 暴力猜测密钥、使用未验证密钥或任意内存偏移；
- 静默上传聊天、数据库、密钥或附件。

自动密钥发现只允许对使用者授权的精确数据库执行只读检查，且候选密钥必须通过
目标数据库的密码学验证。未知版本必须安全停止。

## 校验下载

普通用户不需要手工使用 SHA-256：只需从正式 Release 下载 ZIP，解压后运行
安装器。安装器会自动核对内部 Skill 包，并拒绝哈希不符、文件缺失或包含危险
解压路径的安装包。

正式 Release 仍附带 SHA-256 文件，供希望独立核对下载文件的进阶用户选用。

SHA-256 能发现传输损坏或文件被更改，但不能替代代码签名或维护者身份验证。
