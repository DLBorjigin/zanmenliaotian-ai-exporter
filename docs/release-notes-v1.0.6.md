# v1.0.6 发布说明

这是公开版本 `v1.0.5` 之后的文件消息兼容性修复版。所有源码、运行时、
安装包、说明文档和 GitHub tag 均统一使用 `v1.0.6`。

## 本次修复

- 修复 Weixin 4.1.13.12 将应用消息子类型写入 `local_type` 高 32 位后，文件被误判为系统消息的问题；
- 文件预览和正式导出会分别解析基础类型与应用子类型，缺少 XML 正文时仍能识别文件；
- 本地未下载的文件仍保留在时间线中，并尽可能从安全的消息元数据恢复文件名；
- 保留 v1.0.5 的多数据库合并、发送者归属修复和分类媒体包功能。

## 已验证结果

- 在 Weixin 4.1.13.12 的真实三人群聊范围中正确识别 12 条文件消息；
- 11 个已缓存附件成功打包；
- 1 个微信界面标记“未下载”的文件保留名称和缺失状态，不伪造附件；
- 84 项自动测试及发行包源码一致性测试通过。

## 安全边界

- 默认本地、只读，不写入微信进程，不 Hook，不保存内存转储；
- 密钥、数据库路径和临时明文数据库不会进入导出包；
- 未下载附件不会通过自动控制微信界面强制获取。

## 安装

下载 `微信聊天导出工具-v1.0.6-Windows.zip`，完整解压后双击
`双击安装.cmd`，然后完全关闭并重新打开 Codex。
