# v1.0.8 发布说明

修复微信更新到 4.1.13.65 后，旧版工具无法读取数据库的问题。

## 本次修复

- 新增 4.1.13.65 的精确版本适配，沿用已验证的 WCDB 配置结构读取流程。
- 每个数据库仍需单独通过密钥校验；没有直接放开所有未来微信版本。
- 本机消息、联系人、会话和语音数据库均通过密钥校验、稳定快照、WAL 处理及结构检查。
- 保留 v1.0.7 的本机离线语音转文字，以及多库合并、发送者归属、文件识别和媒体分类修复。
- 安装包内附源码与本次上传源码一致；工具版本、包名及 GitHub tag 统一为 v1.0.8。

## 安装和更新

下载 `微信聊天导出工具-v1.0.8-Windows.zip`，完整解压后双击 `双击安装.cmd`，
看到安装成功后重新打开 Codex。已有用户同样执行此步骤，安装器会备份旧版本。
包内保留离线语音模型，首次转写无需下载模型。

## 验证范围

本次新版本实机验证覆盖四类数据库的读取基础链路；未重新逐条导出所有媒体类型。
新增自动测试覆盖新旧版本的配置解析、未知版本拒绝，以及错误密钥拒绝与清理。
微信未来更新仍可能需要适配。当前仅支持 Windows，尚无经过实机验证的 macOS 版。
